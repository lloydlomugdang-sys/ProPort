<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext is available only in debug builds for emulator and LAN backends. -->
    <base-config cleartextTrafficPermitted="true"/>
</network-security-config>
